---
title: Métodos de la API de Ethereum | Referencia Completa de Métodos JSON-RPC
description: Referencia completa de todos los métodos de la API JSON-RPC de Ethereum soportados, incluyendo endpoints core, debug, trace y web3. Cada método incluye enlaces a documentación, costo multiplicador e información de soporte de red.
keywords: api ethereum, json-rpc, métodos eth, métodos ethereum, api blockchain, ethereum json-rpc, métodos debug, métodos trace, endpoints web3, métodos rpc ethereum
---

# Métodos de la API de Ethereum

Esta página proporciona una lista completa de todos los métodos de la API de Ethereum soportados. Cada método incluye información sobre su propósito y uso. Para obtener información detallada sobre un método específico, haga clic en su nombre para ver la documentación completa.

## Métodos Core de Ethereum (eth\_)

Estos métodos proporcionan funcionalidad básica para interactuar con la blockchain de Ethereum.

<DocsEndpointsTable createLink={(elem) => `/docs/ethereum/core-methods/${elem.key}`} prefixes={['eth_']} />

## Métodos de Depuración (debug\_)

Estos métodos se utilizan para depurar y analizar datos de blockchain en detalle.

<DocsEndpointsTable createLink={(elem) => `/docs/ethereum/debug-methods/${elem.key}`} prefixes={['debug_']} />

## Métodos de Rastreo (trace\_)

Estos métodos proporcionan información detallada de rastreo de transacciones y bloques.

<DocsEndpointsTable createLink={(elem) => `/docs/ethereum/trace-methods/${elem.key}`} prefixes={['trace_']} />

## Métodos de Red (net\_)

Estos métodos proporcionan información sobre el estado de la red y las conexiones.

<DocsEndpointsTable createLink={(elem) => `/docs/ethereum/core-methods/${elem.key}`} prefixes={['net_']} />

## Métodos del Pool de Transacciones (txpool\_)

Estos métodos permiten inspeccionar el estado del pool de transacciones.

<DocsEndpointsTable createLink={(elem) => `/docs/ethereum/core-methods/${elem.key}`} prefixes={['txpool_']} />

## Métodos de Utilidad Web3 (web3\_)

Métodos básicos de utilidad web3 para la interacción general con blockchain.

<DocsEndpointsTable createLink={(elem) => `/docs/ethereum/core-methods/${elem.key}`} prefixes={['web3_']} />
